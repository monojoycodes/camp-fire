## Project.find(...): Always returns an array.

## Project.findOne(...): Always returns a single object (the first one it finds) or null if it finds nothing.

## Project.findById(...): Always returns a single object or null if it finds nothing.